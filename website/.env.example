# PostHog analytics (cookieless). Leave blank to disable analytics entirely,
# for example when running locally. Analytics only ever initializes when
# NEXT_PUBLIC_POSTHOG_KEY is set AND the app is running in production.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=
