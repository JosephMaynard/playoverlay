## What & why

<!-- Briefly describe the change and the reason for it -->

## How was this tested?

<!-- Describe how you verified the change, e.g. which flows you exercised in a running app -->

## Checklist

- [ ] `npm run ts-check` passes
- [ ] `npm run lint` passes
- [ ] `npm test` passes
- [ ] Existing saved config (`config.json`) still loads and behaves unchanged, if this touches stored data
- [ ] Added/updated tests for any new logic
